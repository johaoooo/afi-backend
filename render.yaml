services:
  - type: web
    name: afi-backend-django
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: |
      gunicorn afi_backend.wsgi:application
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: afi-postgres
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "afi-backend-django.onrender.com,localhost,127.0.0.1"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://afi-frontend-nine.vercel.app,https://afi-frontend.vercel.app"
    buildFilter:
      paths:
        - backend/**
databases:
  - name: afi-postgres
    databaseName: afi_collection
    user: afi_user
    plan: free
